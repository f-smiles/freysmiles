'use client'
import React, { useEffect, useRef, useState } from 'react'
import { gsap } from 'gsap'

export const BounceLine = () => {
  return (
    <svg>
      <path fill='none' strokeWidth={1}  />
    </svg>
  )
}